{
	"on_save": [{
	    "cmd": "gs9o_open", "args": {
	    "run": ["sh", 
	        "go vet && golint ."],
	        //"go build . errors && ginkgo test -i --noColor && ginkgo test --noColor && go vet && golint ."],
	    "focus_view": false
	}}],
	"autocomplete_closures": true,
	"complete_builtins": true,
}